# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB (Preview).
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# If you have a local mysql database running on port 3306 then
# Use  your docker-compose.yaml file to use the local port 3307 for the mysql_container databas
# and use port 3307 to reach mysql on mysql_container

DATABASE_URL="mysql://music:music@localhost:3307/music_program?schema=public"
APP_SECRET=some_not_random_secret_that_is_at_least_32_characters
